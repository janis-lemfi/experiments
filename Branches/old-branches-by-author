#!/bin/bash

VAR=$1

if [[ ! -f $VAR ]]; then
	echo "Please provide file with branches"
	exit 0
fi

while read -r line; do
  branch=$(echo "$line" | awk '{split($0,a,"|"); print a[4]}')
	echo "$branch"
	# echo "$line" >> "branches/$author.md"
done <"$VAR"
